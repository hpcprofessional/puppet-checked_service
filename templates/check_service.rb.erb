# This is a stub.
#
# This will be a script that reaches out to a Zabbix server and checks to see
# if some dependent service is reported as up or down, so that we can block
# before managing a service until Zabbix says the dependency is functional.

exit 0
